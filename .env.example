# Example environment variables for Semantica

# Selective Logging Configuration
# Configure which log categories to show (default: minimal logs)
#
# Examples:
# - Show only default (PIPELINE-STATUS and errors):
#   LOG_CATEGORIES=
#
# - Debug file processing:
#   LOG_CATEGORIES=WORKER,INDEXING,QUEUE
#
# - Debug embedder issues:
#   LOG_CATEGORIES=EMBEDDER-*,MEMORY
#
# - Debug encoding issues:
#   LOG_CATEGORIES=ENCODING,FILE-STATUS,INDEXING
#
# - Show all logs (verbose):
#   LOG_CATEGORIES=*
#
# - Mix wildcards and specific categories:
#   LOG_CATEGORIES=EMBEDDER-*,WORKER,INDEXING
#
# Available categories:
# File ops: INDEXING, QUEUE, CLEANUP, WATCHER, FILE-STATUS, REINDEX, ENCODING
# Processing: WORKER, PROCESS-QUEUE, EMBEDDING, EMBEDDING-QUEUE
# Embedder: EMBEDDER, EMBEDDER-EVENT, EMBEDDER-POOL, ISOLATED, IPC-ADAPTER
# System: MEMORY, PERFORMANCE, PROFILING, STATE-MACHINE, CHILD-OUT, CHILD-ERR
# Infra: DATABASE, STARTUP, MOCK, DEPRECATED

LOG_CATEGORIES=

# Performance Profiling
# Set to true to enable performance profiling
PROFILE=false

# Testing Configuration
# Enable model download mocks for E2E testing
E2E_MOCK_DOWNLOADS=false
E2E_MOCK_DELAYS=false

# Custom User Data Path (optional)
# USER_DATA_PATH=/path/to/custom/data